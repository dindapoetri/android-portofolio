<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme Utama -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Warna utama -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
    </style>

    <!-- Style untuk SearchView -->
    <style name="SearchViewStyle">
        <item name="android:background">@drawable/search_background</item>
        <item name="android:iconifiedByDefault">false</item>
        <item name="android:queryHint">Search for accounts</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Style untuk text nama akun -->
    <style name="AccountTextStyle">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Style untuk kode OTP -->
    <style name="OtpCodeTextStyle">
        <item name="android:textSize">32sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#2962FF</item> <!-- biru terang -->
    </style>

    <!-- Style untuk garis pemisah -->
    <style name="DividerStyle">
        <item name="android:layout_height">1dp</item>
        <item name="android:background">#DDDDDD</item>
    </style>
</resources>